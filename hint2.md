What about the radio form fields? These are a bit different from from our text fields.

For form fields with multiple options (e.g., radio, checkbox, or select fields), labels are added to correspond to each available option. So for instance, in our form we have two options for our radio button: yes and no. So we'll need two labels, one for each.