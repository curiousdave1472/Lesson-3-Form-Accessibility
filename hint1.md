Right now our `<p>` element contains useful information about what we want a user to enter in each text field. This is better suited for a `<label>` element.

Read more about the label element here: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/label 