## Form Accessibility
We have a simple form here that asks a user to enter their name, email address, and whether they want to subscribe. But this form doesn't follow good accessibility practices. 

To improve the accessibility of this page, we need to semantically associate labels with each of our form fields.

### Hints
Check out the `hints` folder if you need a hint!

### Bonus Challenge
We can also go one step further to make our form accessible by grouping associated labels (such as our radio labels). Read more about `<fieldset>` here: https://developer.mozilla.org/en-US/docs/Web/HTML/Element/fieldset
